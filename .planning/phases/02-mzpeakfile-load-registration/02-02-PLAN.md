---
phase: 02-mzpeakfile-load-registration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
requirements: [INT-03]
target_repo: /Users/kohlbach/Claude/OpenMS  (branch feature/mzpeak-file-handler)
files_modified:
  - src/openms/source/FORMAT/MzPeakFile.cpp
  - src/tests/class_tests/openms/source/MzPeakFile_test.cpp
---

<objective>
Add the METADATA layer to MzPeakFile::load (M2 = mirror the MzMLHandler::handleCVParam_
accession→setter subset that mzPeak emits). Extend the 02-01 load so each MSSpectrum
carries type (PROFILE/CENTROID), polarity, native id, precursors (isolation window
offsets, activation, EVERY selected ion), and per-spectrum CvParams; and the
MSExperiment (IS-A ExperimentalSettings) carries run-level metadata.
</objective>

<scope>
INCLUDE:
- Per-spectrum scalars from spectra_metadata.parquet (already partially read in 02-01):
  - representation/spectrum_type (MS:1000127 centroid / MS:1000128 profile or MS:1000559) → SpectrumSettings::setType(CENTROID/PROFILE)
  - polarity (MS:1000130 negative / MS:1000129 positive) → InstrumentSettings::setPolarity(POSITIVE/NEGATIVE)
  - id → SpectrumSettings::setNativeID
- Per-spectrum `parameters` CvParam list → CV dispatch helper applyCVParam(context, accession, value, unit, target):
  recognized accession → typed setter (the mzPeak-emitted subset); unrecognized → setMetaValue (encode accession in the key, e.g. "MS:1000XYZ", to keep it — mirrors handleUserParam_).
- Precursor (RDR-10c facets): MSSpectrum holds vector<Precursor>; map EVERY selected ion (not just first);
  isolation window stored as OFFSETS from target m/z (Precursor::setIsolationWindowLowerOffset/UpperOffset,
  setMZ from target); activation (MS:1000133 CID etc. + collision energy MS:1000045) → Precursor activation methods/energy.
- Run-level: parse mzpeak_index.json metadata{} blocks (run / file_description / instrument_configuration_list /
  software_list / data_processing_method_list / sample_list) → ExperimentalSettings (setInstrument, setSample,
  setSourceFiles, software, dateTime). Use the §4/§4a mapping in docs/openms-integration-plan.md.
- base_peak / TIC: computed (getBasePeak/calculateTIC) or setMetaValue — do NOT invent fields.

DEFER: ion mobility (NULL in fixtures → stub); wavelength spectra; chromatogram metadata; M1 shared-helper refactor (Phase 3b).

REFERENCE:
- docs/openms-integration-plan.md §4 (data-model table) + §4a (CV dispatch) + §5 (impedance mismatches — isolation OFFSETS, every selected ion, polarity, computed base-peak/TIC).
- MzMLHandler::handleCVParam_ (src/openms/source/FORMAT/HANDLERS/MzMLHandler.cpp ~line 1415) = the accession→setter source of truth to mirror.
- mzpeak reader metadata_model.cpp (openms-mzpeak) = how to read the precursor/scan/parameters facets from spectra_metadata.parquet (Phase 1 RDR-10b/10c logic — already proven).
</scope>

<ground_truth>
small.mzpeak (pyarrow / Phase-1 spectrum_metadata_test):
- 14 profile (PROFILE, ms_level 1) + 34 centroid (CENTROID, ms_level 2).
- spectrum index 2 (first MS2): precursor isolation target ≈810.789, lower/upper offset 1.0/1.0;
  activation MS:1000133 (CID) + collision energy "35" (UO:0000266); selected_ion m/z ≈810.789428, intensity ≈1994039.125.
- run-level: small.mzpeak's mzpeak_index.json carries metadata{} run/instrument/software/etc. blocks (reference Rust file).
</ground_truth>

<acceptance>
- A profile spectrum: getType()==PROFILE; a centroid: getType()==CENTROID; polarity set; native id set.
- MS2 spectrum index 2: precursors().size()>=1; precursor m/z≈810.789; isolation offsets 1.0/1.0; activation present; selected ion mapped.
- Unrecognized CvParam → present as a meta value (accession-keyed).
- ExperimentalSettings populated from run-level blocks (instrument and/or source file and/or sample non-empty).
- MzPeakFile_test extended with these assertions; libOpenMS rebuilds; test PASSES.
- clang-format-clean + OpenMS conventions.
</acceptance>

<output>
Create .planning/phases/02-mzpeakfile-load-registration/02-02-SUMMARY.md when done.
</output>
