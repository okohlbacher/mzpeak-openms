---
phase: 02-mzpeakfile-load-registration
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
requirements: [INT-02, INT-04]
target_repo: /Users/kohlbach/Claude/OpenMS  (branch feature/mzpeak-file-handler)
files_modified:
  - src/openms/source/FORMAT/FileHandler.cpp
  - src/tests/class_tests/openms/source/MzPeakFile_test.cpp
  - src/tests/class_tests/openms/source/FileHandler_test.cpp (assertions, if present)
---

<objective>
Wire .mzpeak into FileHandler so it resolves by EXTENSION and by CONTENT and
loads through the generic FileHandler API (INT-02), and run the Phase 2 gate
(INT-04). getType(".mzpeak") == MZPEAK; FileHandler().loadExperiment(".mzpeak", exp)
populates the MSExperiment via MzPeakFile.
</objective>

<scope>
- Content detection: in FileHandler::getTypeByContent, detect a .mzpeak archive = a ZIP
  (magic `PK\x03\x04`) that CONTAINS a member named `mzpeak_index.json`. NOTE: the first
  zip member is `spectra_data.parquet`, NOT mzpeak_index.json — so check MEMBERSHIP (e.g.
  ZipArchiveFile::listEntries contains "mzpeak_index.json"), not first-member. Follow the
  existing zip-handling precedent in getTypeByContent (the .d.zip Bruker check). Return MZPEAK.
  Guard cost: only open the zip if the magic bytes say it is a zip.
- Dispatch: in FileHandler::loadExperiment (and getType), add the MZPEAK case →
  MzPeakFile().load(filename, exp). (store dispatch → Phase 3.) Find the existing switch on
  FileTypes::Type and add the case mirroring how MZML/SQMASS dispatch.
- Extension detection already works (FileTypes "mzpeak" registered in Phase 0) — verify.

PHASE GATE (INT-04 + standing gates):
- MzPeakFile_test, FileHandler_test, FileTypes_test all green in the OpenMS harness.
- clang-format diff-verify empty on every Phase-2-edited OpenMS file
  (MzPeakFile.h/.cpp, FileHandler.cpp, FileTypes.h/.cpp, MzPeakFile_test.cpp).
- OpenMS conventions (OPENMS_DLLAPI, SPDX/$Maintainer$).
</scope>

<acceptance>
- FileHandler::getTypeByFileName("x.mzpeak") == FileTypes::MZPEAK.
- FileHandler::getTypeByContent(small.mzpeak) == FileTypes::MZPEAK (works even if the file were
  renamed without the .mzpeak extension).
- FileHandler().loadExperiment(small.mzpeak, exp) → exp.size()==48 (same as MzPeakFile().load).
- MzPeakFile_test extended with a FileHandler-path load assertion; all three tests pass.
- clang-format clean on all Phase-2 OpenMS files.
</acceptance>

<output>
Create .planning/phases/02-mzpeakfile-load-registration/02-03-SUMMARY.md when done.
</output>
