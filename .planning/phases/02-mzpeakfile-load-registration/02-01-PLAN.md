---
phase: 02-mzpeakfile-load-registration
plan: 01
type: execute
wave: 1
depends_on: []
requirements: [INT-01]
target_repo: /Users/kohlbach/Claude/OpenMS  (branch feature/mzpeak-file-handler)
files_modified:
  - src/openms/source/FORMAT/MzPeakFile.cpp
  - src/openms/include/OpenMS/FORMAT/MzPeakFile.h
  - src/openms/source/FORMAT/HANDLERS/MzPeakHandler.cpp (new, optional)
  - src/openms/source/FORMAT/sources.cmake
  - src/tests/class_tests/openms/source/MzPeakFile_test.cpp
  - src/tests/class_tests/openms/data/  (small.mzpeak fixture)
---

<objective>
Implement the DATA LAYER of MzPeakFile::load — open a .mzpeak archive, read its
Parquet tables via OpenMS's ZipRandomAccessFile + Arrow, decode the point-layout
spectra (profile spectra_data.parquet with null-marked m/z reconstructed via the
mz_delta_model; centroid spectra_peaks.parquet), and build a populated
MSExperiment (peaks, RT in seconds, ms_level; sortByPosition + updateRanges).
MzPeakFile_test loads the bundled small.mzpeak fixture and asserts spectrum count,
ms_level split, RT, and peak values against pyarrow ground truth.

Metadata mapping (spectrum_type/polarity/precursor/run-level) = plan 02-02.
FileHandler content-detection + dispatch = plan 02-03.
</objective>

<scope>
INCLUDE (validated now):
- Open .mzpeak: ZipArchiveFile::listEntries + ZipRandomAccessFile::Open(member) → arrow::io::RandomAccessFile.
- Parse mzpeak_index.json (nlohmann/json) to find the spectra data + peaks + metadata table members.
- Read tables: parquet::arrow::OpenFile(raf) + reader->ReadTable(&table) → arrow::Table.
- Decode point-layout spectra: profile (spectra_data.parquet) point columns mz(double, nullable)+intensity(float);
  centroid (spectra_peaks.parquet) mz+intensity. Join points to spectra by spectrum_index.
- Null-marked m/z reconstruction for profile: PORT src/util/null_fill.cpp (C++20-clean) + read the per-spectrum
  mz_delta_model betas from spectra_metadata.parquet; reconstruct interior null m/z (mirror mzpeak reader RDR-5).
- Converter: MSSpectrum push_back(Peak1D{mz,int}); setRT(seconds); setMSLevel; sortByPosition();
  MSExperiment::addSpectrum; updateRanges() MANDATORY.
- Copy null_fill + numpress cores into OpenMS (e.g. source/FORMAT/HANDLERS/ or a private util) under OpenMS style,
  C++20, for reuse here + Phase 4 chunked/numpress.

DEFER (later plan / fixture-gated):
- Chunked + numpress layouts (small.chunked/small.numpress) → a follow-up; small.mzpeak is point-layout.
- Full metadata mapping (02-02). Chromatograms (Phase 4 territory; load may skip chromatogram tables for v1).
- Ion mobility (NULL in fixtures; stub).

REFERENCE (port from these known-good mzpeak-lib files in openms-mzpeak repo — DO NOT depend on the lib, port the logic):
- src/util/metadata_model.cpp (read_mz_delta_models; per-row table access idioms)
- src/util/null_fill.cpp + include/mzpeak/util/null_fill.h (reconstruct_null_mz, predict_delta, estimate_median_delta)
- include/mzpeak/util/encoding.h (point decode), src/spectrum.cpp (intensity float32, mz double)
- src/util/parquet.cpp (column resolution by name)
</scope>

<ground_truth>
small.mzpeak (pyarrow-verified): 48 spectra total — 14 profile (ms_level 1, in spectra_data.parquet) +
34 centroid (ms_level 2, in spectra_peaks.parquet). Spectrum index 0 (profile): after null-mz reconstruction
matches mzpeak reader read_spectrum output exactly (no interior zero-mz). Spectrum index 0 intensity[0..]
= 1938.12, 2572.84, ... RT is in seconds. Use the mzpeak C++ reader (build/ in openms-mzpeak: `MzPeak::open`)
or pyarrow as the oracle to capture exact assertion values.
</ground_truth>

<acceptance>
- MzPeakFile().load(small.mzpeak, exp) yields exp.size() == 48; exactly 14 ms_level==1 + 34 ms_level==2.
- Profile spectrum 0: non-empty peaks, m/z strictly ascending (sorted), NO interior zero m/z (reconstruction worked),
  intensity[0] within 1e-3 of ground truth.
- Centroid spectrum (a ms_level==2 one): non-empty peaks matching pyarrow within tolerance (1e-9 mz / 1e-6 int).
- RT set in seconds; updateRanges() called (exp.getMinMZ()/getMaxRT() sane, non-default).
- libOpenMS rebuilds with the new code; MzPeakFile_test compiles, links, and PASSES in the OpenMS harness.
- All new/edited OpenMS files clang-format-clean + OpenMS conventions (OPENMS_DLLAPI, SPDX/$Maintainer$ headers).
</acceptance>

<verification>
- make -C ~/openms_build -j16 OpenMS && make -C ~/openms_build -j16 MzPeakFile_test && run it → PASS.
- Build/link recipe: .planning/intel/openms-build-recipe.md.
</verification>

<output>
Create .planning/phases/02-mzpeakfile-load-registration/02-01-SUMMARY.md when done.
</output>
