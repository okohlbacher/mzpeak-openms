---
phase: 01-reader-lossless-map-prerequisites
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - include/mzpeak/spectrum_metadata.h
  - src/util/metadata_model.cpp
  - test/spectrum_metadata_test.cpp
autonomous: true
requirements: [RDR-9b]
must_haves:
  truths:
    - "SpectrumMetadata exposes auxiliary_arrays as a typed std::vector<AuxiliaryArray>"
    - "AuxiliaryArray carries its CV name term, data_type, compression, unit, parameters, data_processing_ref, and decoded values"
    - "auxiliary_arrays reads as an empty list (no crash) on every bundled fixture — structural round-trip validated"
    - "A documented deferred note states value-level validation awaits a fixture with populated aux data"
    - "Full meson test suite AND scripts/e2e.sh stay green (phase gate)"
  artifacts:
    - path: "include/mzpeak/spectrum_metadata.h"
      provides: "AuxiliaryArray struct + auxiliary_arrays vector on SpectrumMetadata"
      contains: "struct AuxiliaryArray"
    - path: "src/util/metadata_model.cpp"
      provides: "auxiliary_arrays decode skeleton (structural)"
      contains: "auxiliary_arrays"
    - path: "test/spectrum_metadata_test.cpp"
      provides: "Structural empty-list assertion for aux arrays"
      contains: "auxiliary_arrays"
  key_links:
    - from: "src/util/metadata_model.cpp"
      to: "read_cv_params_from_list (plan 01)"
      via: "aux name + parameters CvParam decode"
      pattern: "read_cv_params_from_list"
---

<objective>
Implement RDR-9b: a typed accessor exposing each spectrum's `auxiliary_arrays`
(name/CV term + data_type/compression/unit + parameters + decoded values), so
Phase 2 can map them to OpenMS `MSSpectrum::FloatDataArrays`.

`number_of_auxiliary_arrays == 0` in every bundled fixture, so this is validated
STRUCTURALLY (the schema fields parse + an empty-list round-trip), with an
explicit deferred note that value-level validation awaits a fixture carrying aux
data. Per the orchestrator's scope decision, this does NOT block the phase.

This plan also runs the FINAL phase gate: full `meson test` + `scripts/e2e.sh`
green, and a final clang-format diff-verify across all files this phase touched.

Pure standalone mzpeak-lib work on branch `writer_test`. NO OpenMS code.

Purpose: Phase 2 FloatDataArrays mapping needs a typed aux-array accessor.
Output: AuxiliaryArray struct, the decode skeleton, structural test, phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/mzpeak-openms-01-reader-lossless-map-prerequisites/01-CONTEXT.md
@.planning/phases/mzpeak-openms-01-reader-lossless-map-prerequisites/01-RESEARCH.md

<read_first>
- include/mzpeak/spectrum_metadata.h  (extended by plans 01/02; add AuxiliaryArray here)
- src/util/metadata_model.cpp  (read_spectra_metadata; reuse read_cv_params_from_list + offset idiom)
- include/mzpeak/util/encoding.h  (existing secondary/aux column decode patterns to mirror)
- include/mzpeak/run_metadata.h  (CvParam for aux name + parameters)
- test/spectrum_metadata_test.cpp  (extend with the structural aux case)
- .planning/phases/mzpeak-openms-01-reader-lossless-map-prerequisites/01-02-SUMMARY.md
</read_first>

<schema>
<!-- spectrum.auxiliary_arrays: large_list<struct>; EMPTY (0 elements) in ALL bundled fixtures. -->
<!-- spectrum.number_of_auxiliary_arrays: uint32, == 0 in all rows of all fixtures. -->
auxiliary_arrays item struct children (exact names):
  data : large_list<uint8>         (raw bytes; decode by data_type+compression)
  name : struct<CvParam>           (same shape as parameters items: value-union/accession/name/unit)
  data_type : string               (Arrow dtype name, lowercase e.g. "float32" — opaque, NOT a PSI accession; Pitfall 5)
  compression : string             (e.g. "none")
  unit : string
  parameters : large_list<CvParam>
  data_processing_ref : large_string
</schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define AuxiliaryArray struct + accessor</name>
  <files>include/mzpeak/spectrum_metadata.h</files>
  <action>
In `include/mzpeak/spectrum_metadata.h`, add `struct AuxiliaryArray` with `/** */`
Doxygen, snake_case fields, reusing CvParam:
`CvParam name;` (CV term identifying the array),
`std::string data_type;` (Arrow dtype name, e.g. "float32" — opaque string, NOT
routed through any PSI enum; Pitfall 5),
`std::string compression;`,
`std::string unit;`,
`std::vector<CvParam> parameters;`,
`std::string data_processing_ref;`,
`std::vector<float> values;` (decoded; empty when data is empty).
Add member `std::vector<AuxiliaryArray> auxiliary_arrays;` to `SpectrumMetadata`,
with a Doxygen note: "Empty in all bundled fixtures
(number_of_auxiliary_arrays==0); value-level validation deferred to a fixture with
populated aux data (RDR-9b)." Interface-first: contract only, no reads yet.
  </action>
  <verify>
    <automated>cd builddir && meson compile 2>&1 | tail -5 && echo "header compiles"</automated>
  </verify>
  <done>AuxiliaryArray struct + auxiliary_arrays member compile; deferred note present in Doxygen.</done>
</task>

<task type="auto">
  <name>Task 2: Decode auxiliary_arrays (structural skeleton) + structural test</name>
  <files>src/util/metadata_model.cpp, test/spectrum_metadata_test.cpp</files>
  <action>
In `read_spectra_metadata` per-row loop (the spectrum-struct pass), decode
`spectrum.auxiliary_arrays`: GetFieldByName("auxiliary_arrays") → if absent/NULL,
leave empty (every bundled row is empty → loop body never runs, cost zero). Cast
to `arrow::LargeListArray`; iterate `[value_offset(r), value_offset(r+1))` (same
idiom as read_cv_params_from_list); each item struct → build an AuxiliaryArray:
`name` via the plan-01 single-CvParam logic on the item's "name" struct (factor
the extract-one-CvParam body out of read_cv_params_from_list into a reusable
helper if it is not already separable); `data_type`/`compression`/`unit` via
get_string; `parameters` via read_cv_params_from_list(item, "parameters", k);
`data_processing_ref` via get_string. For `values`: read the `data`
large_list<uint8> raw bytes and decode per `data_type`+`compression` — implement a
small local switch on the lowercase data_type ("float32","float64","int32" →
convert to float; default → leave empty + comment). Treat data_type as opaque
(Pitfall 5): do NOT route through the PSI::DataType enum. BYTE-LENGTH GUARD
(mandatory): before reinterpreting/converting the raw bytes for any arm of the
switch, assert `data.size() % sizeof(element_type) == 0` for the element type
selected by `data_type` (4 for float32/int32, 8 for float64); on mismatch leave
`values` empty and do NOT reinterpret the bytes (a truncated/misaligned buffer
must never be reinterpreted). Add a code comment noting
this path is exercised only structurally because all bundled fixtures have
number_of_auxiliary_arrays==0, so value-level correctness awaits a real aux
fixture.

Append to `m.auxiliary_arrays`.

Extend `test/spectrum_metadata_test.cpp` with a structural case: for every spectrum
in small.mzpeak (and one check on has_uv.mzpeak), assert `auxiliary_arrays` is
empty and that reading it does not throw/crash — i.e. the empty-list round-trip
holds and the schema fields parse. Document in a test comment that value-level
aux assertions are deferred pending a populated fixture.
  </action>
  <verify>
    <automated>cd builddir && meson test spectrum_metadata 2>&1 | tail -6</automated>
  </verify>
  <done>auxiliary_arrays decodes structurally (empty list, no crash) on all bundled fixtures; the raw-bytes decode switch contains the explicit `data.size() % sizeof(element_type) == 0` guard (leaving values empty on mismatch, never reinterpreting); deferred-validation note in code + test; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full meson test + e2e + final style diff-verify</name>
  <files>include/mzpeak/spectrum_metadata.h, src/util/metadata_model.cpp, include/mzpeak/util/metadata_model.h, test/spectrum_metadata_test.cpp</files>
  <action>
Run the standing per-phase gates that close Phase 1:
1. `clang-format -i` (style=file) on EVERY file edited across all three plans of
   this phase, then diff-verify each (`clang-format --style=file f | diff f -`
   EMPTY, not --dry-run) and confirm editorconfig (final newline, no trailing
   whitespace, LF).
2. Full unit suite: `meson test -C builddir` must be entirely green.
3. End-to-end: `scripts/e2e.sh` must stay green.
If any check fails, fix and re-run before declaring done. (Cross-AI adversarial
review of the plan — Codex + Vibe — is an orchestrator-level gate run BEFORE
execution, not a task here.)
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && for f in include/mzpeak/spectrum_metadata.h src/util/metadata_model.cpp include/mzpeak/util/metadata_model.h test/spectrum_metadata_test.cpp; do clang-format --style=file "$f" | diff "$f" - || { echo "DRIFT $f"; exit 1; }; done && meson test -C builddir 2>&1 | tail -5 && scripts/e2e.sh 2>&1 | tail -10</automated>
  </verify>
  <done>All edited files clang-format-clean; full meson test suite green; scripts/e2e.sh green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parquet file → reader | Untrusted auxiliary_arrays list + raw uint8 data bytes crossed into typed structs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | aux `data` raw-byte decode | mitigate | Switch on opaque data_type; unknown type → empty values, never reinterpret blindly; length-check bytes against element width before converting |
| T-03-02 | Denial of Service | large_list aux iteration | accept | Bounded by Arrow offsets; bundled fixtures have 0 aux arrays (loop never runs) |
| T-03-03 | Repudiation | unvalidated decode path | accept | Documented deferred note in code + test; structural validation only until a populated fixture exists |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs — source edit against existing deps |
</threat_model>

<verification>
- `cd builddir && meson test spectrum_metadata` — RDR-9b structural case green
  (auxiliary_arrays empty + no crash on all bundled fixtures).
- `meson test -C builddir` — full unit suite green (phase regression gate).
- `scripts/e2e.sh` — green (phase e2e gate).
- clang-format diff-verify empty on all phase-edited files.
- Deferred note present in both code and test (value-level aux validation awaits a
  populated fixture).
</verification>

<success_criteria>
- SpectrumMetadata.auxiliary_arrays is vector<AuxiliaryArray> with the full
  per-array schema (name term, data_type, compression, unit, parameters,
  data_processing_ref, values).
- Decode is structurally validated (empty-list round-trip, schema parses) with a
  documented deferred note.
- Full meson test + scripts/e2e.sh green; all phase-edited files clang-format-clean.
</success_criteria>

<output>
Create `.planning/phases/01-reader-lossless-map-prerequisites/01-03-SUMMARY.md` when done.
</output>
